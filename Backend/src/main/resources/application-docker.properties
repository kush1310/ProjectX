# ===================================================================
# Charusat Needs — Local Docker Profile (docker)
# Activated via: SPRING_PROFILES_ACTIVE=docker
# Configured for local docker-compose environment (Postgres, Redis, Mailpit)
# ===================================================================

spring.datasource.url=jdbc:postgresql://postgres:5432/charusatneeds
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

server.port=8000
server.forward-headers-strategy=native

# JWT Secrets
jwt.secret=${JWT_SECRET:charusatneedsdevsecretkeyminimum256bitsneededforsecurity}
jwt.expiration=900000

# CORS
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:80

# Field & Payload Encryption
security.encryption.key=${FIELD_ENCRYPTION_KEY:CharusatNeeds2026!AES256FieldKey}
security.payload.encryption.key=${PAYLOAD_ENCRYPTION_KEY:CharusatNeedsPayloadKey2026!!}

# Razorpay Test Credentials
razorpay.key.id=${RAZORPAY_KEY_ID:}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:}

# Mail Sandbox (Mailpit)
spring.mail.username=canteen-alerts@charusat.edu.in
spring.mail.password=
spring.mail.from=canteen-alerts@charusat.edu.in
spring.mail.from-name=Charusat Needs Local Docker
app.frontend.url=http://localhost:80

# Redis Cache
spring.data.redis.host=redis
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.ssl.enabled=false
redis.host=redis
redis.port=6379
redis.password=
redis.ssl=false
redis.enabled=true
redis.default-ttl-seconds=300

# Logging
logging.level.root=INFO
logging.level.com.charusat.canteen=DEBUG
